import { userDetails } from "./index2";
enum state {
    waitingForPA = 1,
    AssignToSME, //2
    ResponseCompleted  // 3

}
console.log(state[2]);
let dataFromNewFile = new userDetails.data();
userDetails.Address
dataFromNewFile.sample();